{{- if .Values.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ml.fullname" . }}
  labels:
    app: {{ include "ml.name" . }}
    chart: {{ include "ml.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
{{- end }}
